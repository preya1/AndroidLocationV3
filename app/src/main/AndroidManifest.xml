<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecv4">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalizareComandaActivity"></activity>
        <activity android:name=".CosActivity" />
        <activity android:name=".ProdusDetaliiActivity" />
        <activity android:name=".SetariActivity" />
        <activity
            android:name=".AcasaActivity"
            android:label="@string/title_activity_acasa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminAdaugaProdusActivity" />
        <activity android:name=".AdminCategorieActivity" />
        <activity android:name=".InregistrareActivity" />
        <activity android:name=".LogareActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>